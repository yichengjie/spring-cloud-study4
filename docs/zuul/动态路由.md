1. 新建动态路由表
    ```text
    CREATE TABLE dynamic_zuul_route(
       id             INT            NOT NULL   PRIMARY KEY AUTO_INCREMENT,
       path           VARCHAR(100)   NOT NULL,
       service_id     VARCHAR(20)  ,
       url            VARCHAR(100) ,
       strip_prefix   INT,
       retryable      INT,
       ENABLE         INT,
       description    VARCHAR(50)
    ) ;
    
    INSERT INTO dynamic_zuul_route(path,service_id,url,strip_prefix,retryable,ENABLE,description)
    VALUES('/baidu/**',NULL,'http://www.baidu.com',1,0,1,'重定向百度') ;
    
    INSERT INTO dynamic_zuul_route(path,service_id,url,strip_prefix,retryable,ENABLE,description)
    VALUES('/client/**',NULL,'http://localhost:7070',1,0,1,'url') ;
    
    INSERT INTO dynamic_zuul_route(path,service_id,url,strip_prefix,retryable,ENABLE,description)
    VALUES('/client-a/**','client-a',NULL,1,0,1,'serviceId') ;
    ```
2. 
    